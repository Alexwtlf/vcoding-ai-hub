const MissionSection = () => {
  return (
    <section className="py-20 px-4 sm:px-6 lg:px-8">
      <div className="max-w-3xl mx-auto text-center">
        <blockquote className="text-xl sm:text-2xl md:text-3xl font-medium text-foreground leading-relaxed">
          <span className="gradient-text">"</span>
          AI changed how fast we build.
          <br />
          It didn't change how hard it is to get users.
          <br />
          <span className="text-muted-foreground">VcodingList exists to close that gap.</span>
          <span className="gradient-text">"</span>
        </blockquote>
      </div>
    </section>
  );
};

export default MissionSection;
